# Cosa sostituire prima di pubblicare

Sito statico, nessuna build richiesta: apri `index.html` col doppio click o servilo con un qualsiasi server statico (es. `palestra-landing/serve.ps1`, porta 8793).

## Contenuti da inserire
- **Logo**: fatto — `img/logo.webp` è il logo reale, usato nell'header di ogni pagina (`<img class="brand-logo-img">`, con un disco bianco dietro per restare leggibile sulla navbar scura). Se in futuro lo sostituisci con un altro file, aggiorna il percorso `img/logo.webp` in tutte e 5 le pagine (o mantieni lo stesso nome file).
- **Hero** (`index.html`): fatto — niente più video, ora è un carosello di 3 foto reali (`img/hero scorrimento 1.jpeg`, `img/Hero scorrimento 2.jpeg`, `img/hero scorrimento 3.jpeg`) a schermo intero, con scorrimento automatico ogni 5 secondi (dissolvenza fluida) più frecce e pallini per lo scorrimento manuale in basso su desktop. Per aggiungere o togliere foto: aggiungi/rimuovi un `<div class="hero-slide" style="background-image:url('img/...')">` dentro `#heroSlider` in `index.html` — il JS (`js/main.js`) si adatta da solo al numero di foto presenti. Il testo introduttivo ("Sala pesi, macchinari...") e il pulsante "Dove si trova" sono stati rimossi su richiesta (ripetevano contenuti già presenti altrove nella pagina): l'hero ora ha solo titolo e il pulsante "Abbonati ora". L'indirizzo (`.hero-address`) è posizionato in basso a sinistra sull'hero **solo su desktop**; su mobile è nascosto (era ripetuto poco sotto, nella sezione "Dove ci troviamo"). Su mobile sono nascoste anche le frecce di scorrimento manuale: l'interazione avviene scorrendo con il dito (swipe reale via `touchstart`/`touchend`, gestito in `js/main.js`), i pallini restano visibili come indicatore.
- **Foto**: fatto — `foto.html` mostra ora 4 foto reali (sala pesi, zona corpo libero/macchinari, staff, reception). Non ci sono più riquadri segnaposto "Foto in arrivo": la griglia (`.photo-grid` in `foto.html`) mostra solo le foto realmente presenti. Per aggiungerne altre in futuro basta aggiungere un nuovo `<div class="photo-item"><img src="img/..." alt="..."></div>` nell'elenco — si aggiunge in coda, senza bisogno di segnaposto.
- **Foto macchinari** (`macchinari.html`): le emoji sono state rimosse su richiesta (stonavano con le foto reali del resto del sito) — le card ora sono solo testo (categoria, nome, descrizione). Se in futuro vuoi foto reali per ogni attrezzo, la struttura HTML lo permette facilmente (basta reintrodurre un blocco immagine dentro `.machine-info`).
- **Telefono, email**: fatto — numero reale 328 479 0333 ed email revolutionsportgo@gmail.com impostati in tutte le pagine (footer "Contatti", sezione "Dove si trova" in home, `privacy.html`). Sia il telefono che l'email sono ora link cliccabili (`tel:+393284790333` e `mailto:revolutionsportgo@gmail.com`), anche nei pulsanti "Scegli questo piano" di `abbonati.html` (prima puntavano al numero segnaposto).
- **Indirizzo**: già impostato su "Via Brigata Casale, 32 — 34170 Gorizia (GO)" in tutte le pagine, mappa e link indicazioni inclusi. Aggiorna se cambia.
- **Orari** (`js/main.js`, oggetto `HOURS`): fatto — orari reali impostati: Lun–Ven 09:00–21:00, Sabato 09:00–13:00, Domenica chiuso. Alimentano sia il badge "Aperto/Chiuso" in hero sia la scheda espandibile orari (in home) sia il testo nel footer di tutte le pagine.
- **Prezzi abbonamento**: fatto — `abbonati.html` ha 4 piani (giornaliero €5, mensile €45, 6 mesi €215, 12 mesi €345, quest'ultimo evidenziato come "Più conveniente"). Aggiorna gli importi lì se cambiano — se cambi il prezzo di un piano ricordati di ricalcolare a mano anche l'equivalente mensile e il "Risparmi €" nella stessa card (non sono calcolati automaticamente): per il piano 6 mesi, ad esempio, "equivalenti a X/mese" = prezzo ÷ 6, e "Risparmi €" = (prezzo mensile × 6) − prezzo del piano. La card riepilogativa finale ("Pronto a iniziare ad allenarti?" con Chiamaci/Scrivici/Paga online) è stata rimossa su richiesta perché ripeteva le stesse info dei 4 piani sopra — **di conseguenza il sito non ha più pulsanti diretti "Chiamaci"/"Scrivici"/"Paga online"**: i contatti restano comunque disponibili in fondo a ogni pagina (footer: telefono, email, indirizzo) e in ogni card prezzo c'è comunque un pulsante "Scegli questo piano" (link `tel:`). Su schermi sotto i 760px, tra il titolo della sezione e il resto, è mostrata in evidenza una card singola (`.mobile-featured-card`, nascosta su desktop/tablet): "Accesso alla sala pesi" per i servizi, "Mensile — €45/mese" (con pulsante "Scegli questo piano") per gli abbonamenti. Sotto, sia "Servizi inclusi" che "Abbonamenti" sono racchiusi in una scheda a comparsa (elemento `<details>`, come lo stesso meccanismo usato per gli orari in home): di default è visibile solo un pulsante ("Vedi tutti i servizi" / "Vedi tutti gli abbonamenti"); toccandolo si apre e mostra tutte le card come su desktop, e il pulsante diventa "Nascondi servizi" / "Nascondi abbonamenti" per richiuderlo (nessun contenuto è stato tolto, solo nascosto finché non si apre). Su desktop/tablet il comportamento è invariato: griglia di card sempre visibile, nessuna card in evidenza, nessun pulsante.
- **Paga online** (`abbonati.html`): il pulsante "Paga online" non è più presente (rimosso insieme alla card finale, vedi sopra). Se in futuro serve un pagamento online, va reintrodotto come pulsante a sé con un link di pagamento sicuro reale (es. Stripe/PayPal), non salvare mai dati di carta sul sito.
- **Recensioni**: fatto — sono ora 5 recensioni reali (Fabio, Pierpaolo, Mauro, Giulio, Giuseppe, tutte 5 stelle) inserite manualmente in `js/main.js` (array `REVIEWS`). La sezione recensioni non è più una pagina a parte: ora è integrata direttamente in **home**, sotto "Seguici sui social" (`index.html`, `id="recensioni"`). Il link nel footer di tutte le pagine è stato rimosso di conseguenza (era "poco visibile" in fondo alla pagina). `recensioni.html` esiste ancora e funziona se visitata direttamente, ma non è più linkata da nessuna parte del sito. Per aggiungere altre recensioni in futuro: aggiungi un nuovo oggetto `{name, rating, date, text}` all'array `REVIEWS` — non serve altro, compare automaticamente nel carosello (ordinato per valutazione più alta). Il campo `date` è impostato genericamente a "Recensione Google" perché non abbiamo la data esatta dei post originali.
- **Social**: fatto — Facebook e Instagram collegati ai profili reali su tutte le pagine (bottoni "Segui" in home + icone footer). Il pulsante "Lascia una recensione su Google" (in home e in `recensioni.html`) è impostato sulla pagina dei risultati Google per "asd revolution sport": non è il link diretto "scrivi una recensione" (serve il `placeid` reale della palestra per quello), ma è comunque cliccabile e porta l'utente sulla scheda Google della palestra da cui può lasciare la recensione. Da sostituire con il link diretto `https://search.google.com/local/writereview?placeid=...` quando si avrà il placeid.
- **Corsi ed eventi**: fatto — non è più una sezione della home, ma una pagina dedicata `aggiornamenti.html`, raggiungibile dal link "Aggiornamenti" in navbar (e "Corsi ed eventi" nel footer) su tutte le pagine. C'è ora una card reale (Circuit Training, vedi dettagli più sotto); le altre due card segnaposto sono state rimosse in attesa di contenuti veri su altri corsi/eventi.
- **Area profilo**: eliminata. `profilo.html` (login/registrazione dimostrativi, mai funzionanti) è stata cancellata insieme al relativo codice JS e agli stili (`.auth-*`, `.form-field`, `.form-check`, `.form-note`, `.preview-grid`, `.dev-note.center`), perché il sito non ha bisogno di un login. Il sito ora non contiene nessun `<form>`. Se in futuro servisse davvero un'area personale, va costruita da zero con un backend vero (password salvate solo come hash, pagamenti gestiti da un processore certificato tipo Stripe/PayPal, mai dati di carta sul sito) e l'informativa privacy va aggiornata prima della messa online. Una copia della vecchia pagina è nel backup del 12/09/2026.
- **Privacy e Cookie** (`privacy.html`): riscritta in versione estesa (12 sezioni) basata su come funziona realmente il sito: premessa, titolare, categorie di dati (contatti, iscrizione/tesseramento, certificato medico come dato particolare art. 9, log tecnici dell'hosting, elenco esplicito di ciò che il sito **non** raccoglie, minori), finalità con base giuridica e tempi di conservazione per ciascuna, natura del conferimento, destinatari, trasferimenti extra-UE, misure di sicurezza, diritti artt. 15-22 + reclamo al Garante, assenza di profilazione/decisioni automatizzate, sezione cookie dettagliata (chiave `revolutionSportCookieConsent` con valori e durata, Google Maps, link social senza pixel, come cancellare), modifiche e data di ultimo aggiornamento. **Resta una bozza da far validare da un avvocato/DPO prima della pubblicazione**: l'avviso in cima alla pagina lo dice esplicitamente. Le parti che solo l'associazione può completare sono marcate nel testo con `[DA COMPLETARE]` — cercarle in pagina: codice fiscale/P.IVA, ente di affiliazione e assicurazione, età minima di iscrizione, modalità e durata di conservazione del certificato medico, retention per le richieste informative, eventuale DPO/referente privacy, eventuale cambio di hosting/dominio. Da valutare anche se serve un'informativa separata per la videosorveglianza, se presente in palestra. Link "Privacy e Cookie" nel footer di tutte le pagine. Stili dedicati in `css/style.css` sotto "Pagine legali" (`.legal-page`, `.legal-updated`) perché il reset globale toglie i pallini alle liste.
- **Mappa e consenso cookie** (`index.html`): risolto — la mappa Google Maps **non è più nell'HTML** e non viene più caricata all'apertura della pagina. L'iframe viene creato via JS (`js/main.js`, blocco "Mappa caricata solo su consenso") e solo in due casi: se l'utente ha già scelto "Accetta" sul banner, oppure se premuto il pulsante "Carica la mappa" nel riquadro che compare al posto della mappa (caricamento valido per la sola visita in corso, non cambia la preferenza salvata). Premendo "Accetta" la mappa viene caricata subito, senza ricaricare la pagina. Così "Rifiuta" ha un effetto tecnico reale: nessuna richiesta ai server di Google, nessun IP comunicato. L'URL della mappa è nell'attributo `data-map-src` di `#mapFrame`: se cambia l'indirizzo, va aggiornato lì. Il riquadro di consenso è stilizzato da `.map-consent` in `css/style.css`. I punti 7, 11.2 e 11.5 dell'informativa descrivono questo comportamento.
- **Banner cookie**: fatto — compare in basso su tutte le pagine al primo accesso (dopo una breve animazione), con pulsanti "Accetta" e "Rifiuta". La scelta viene salvata in `localStorage` (`revolutionSportCookieConsent`, valori `"accepted"`/`"rejected"`) e il banner non ricompare più finché non viene cancellata la memoria del browser. Attualmente il sito non usa cookie di profilazione/marketing: se in futuro ne aggiungi (es. Google Analytics), caricali solo quando `localStorage.getItem('revolutionSportCookieConsent') === 'accepted'`.

## Note tecniche
- Nessuna dipendenza esterna nel codice (niente CDN/font esterni), coerente con l'app `focus-sprint` già presente nel progetto.
- Il credito "Sito realizzato da Daniele Kodermaz" (link al portfolio) compare due volte nel footer di ogni pagina: nella colonna "Contatti" e nella riga finale accanto al copyright/Privacy e Cookie.
- Header: tema scuro, fisso in alto (`position: fixed`), stesso colore durante tutto lo scroll su ogni pagina; sotto i 980px (tablet compresi) diventa un menu a comparsa (anch'esso scuro) — soglia alzata da 760px a 980px perché sui tablet in verticale (es. 768px) la barra orizzontale non aveva spazio e il pulsante "Iscriviti ora" andava a capo/tagliato.
- Layout pensato per un riferimento desktop 1440×900 (container a 1280px); resta comunque responsive su schermi più piccoli.
- Mappa: l'iframe e il link "Ottieni indicazioni" puntano all'indirizzo esatto della palestra (nessuna API key richiesta, embed pubblico di Google Maps).
- Orari: scheda a comparsa (`<details>`), mostra solo il giorno corrente per default; cliccandoci sopra si espande con l'elenco completo della settimana e le chiusure straordinarie.
- Filtro macchinari (ora con 7 categorie: Braccia, Gambe, Petto, Schiena, Addominali, Cardio, Corpo libero — 32 macchinari totali) e carosello recensioni sono gestiti in `js/main.js`, senza librerie esterne. Alcuni macchinari compaiono in più categorie (es. Manubri e Cavi in Braccia/Petto/Schiena): basta separare le categorie con uno spazio nell'attributo `data-category`.
- Se in futuro serve il bilinguismo IT/SI, si può aggiungere uno switch di lingua: al momento i testi sono solo in italiano.
- Favicon: impostata su tutte le pagine (`img/logo.webp`).
- Pallini dei caroselli (hero e recensioni): ogni pallino è un pulsante da 24×24px (30×24px quello attivo), la soglia minima per il tocco; il pallino visibile da 8px (o la barretta da 22px dell'attivo) è disegnato dal `::before`. Se si cambia l'aspetto dei pallini, modificare il `::before`, non le dimensioni del pulsante.
- **Tema forzato chiaro**: il sito è pensato solo per il tema chiaro. Su alcuni telefoni (soprattutto Android con "tema scuro forzato per i siti web" attivo), senza questa impostazione elementi nativi come i pulsanti a comparsa (`<details>/<summary>`, es. "Vedi tutti i servizi") potevano apparire scuri finché non venivano toccati. Risolto aggiungendo `<meta name="color-scheme" content="light">` in ogni pagina e `color-scheme: light;` in cima a `css/style.css`: dice esplicitamente al browser di non applicare mai un tema scuro automatico al sito.
- Lightbox foto (`foto.html`): cliccando una foto della galleria si apre ingrandita e centrata con una dissolvenza/zoom morbido; clic fuori dai bordi, sul pulsante ✕, sulla foto ingrandita stessa (per richiuderla velocemente) o tasto Esc la richiude con la stessa animazione al contrario. Si attiva automaticamente su ogni `<img>` dentro `.photo-item`, quindi le nuove foto aggiunte a `foto.html` la useranno già senza modifiche extra. **Disattivata sotto 760px** (mobile) solo in `foto.html`: a quella larghezza la foto ingrandita risulterebbe della stessa dimensione della copertina, quindi non ha senso aprirla (il controllo è in `js/main.js`, `window.innerWidth <= 760`).
- **Stessa lightbox su `aggiornamenti.html`**: la locandina del corso (`.event-thumb img`) è cliccabile per ingrandirla, su qualsiasi dimensione di schermo (qui non è disattivata su mobile, perché a differenza delle miniature di `foto.html` la locandina ingrandita è comunque più leggibile del riquadro nella card). Si richiude cliccandola di nuovo, con il pulsante ✕, cliccando fuori o con Esc — stessa lightbox condivisa, riusa lo stesso markup (`#lightbox`/`#lightboxImg`/`#lightboxClose`) copiato in fondo alla pagina.

## Sicurezza (16/09/2026)
**Cosa è già nel codice**
- **Content Security Policy** in `<head>` di tutte le 8 pagine (`<meta http-equiv="Content-Security-Policy">`): il browser esegue solo script e stili che arrivano dal sito stesso, accetta immagini solo dal sito e iframe solo da Google Maps; niente plugin, niente form, niente `<base>` esterni. Verificata: uno script esterno e un `onerror` iniettati di prova vengono bloccati, il sito non genera nessuna violazione.
- **Regole per non romperla**:
  - niente `style="..."`, `onclick=`/`onerror=` o `<script>` con codice dentro l'HTML: usare classi in `css/style.css` (sezione "Classi che sostituiscono gli stili inline") e codice in `js/main.js`;
  - per aggiungere un servizio esterno (Google Analytics, un video YouTube, un font Google, un modulo di terzi) va **aggiornata la policy in tutte le pagine**, e quasi sempre anche il banner cookie e l'informativa;
  - il blocco `<script type="application/ld+json">` della home è un dato, non codice: la policy non lo blocca.
- **Referrer**: `strict-origin-when-cross-origin` (i siti esterni vedono solo il dominio di provenienza, non la pagina).
- **Mappa isolata**: l'iframe di Google ha `sandbox` (non può navigare la nostra pagina né inviare moduli) e un referrer ristretto.
- **Link esterni**: tutti con `rel="noopener"`. Il link "Lascia una recensione su Google" è stato ripulito dai parametri del browser di chi l'aveva copiato (`rlz`, `gs_lcrp`, `sourceid`…); ora è `google.com/search?q=A.S.D.+Revolution+Sport+Gorizia`.
- **Nessun dato sensibile nel codice**: niente chiavi API, token, password o form. Tutti i pulsanti hanno `type="button"`.
- **Logo**: il ripiego con la sigla "RS", se l'immagine non si carica, ora è in `js/main.js` (prima era un `onerror` inline).
- **Server locale `serve.ps1`**: ascolta solo su `localhost`; corretto un controllo dei percorsi che, per un difetto di prefisso, avrebbe servito file di cartelle vicine con nome simile (es. `palestra-landing-altro`). Richieste come `/../../Windows/win.ini` ricevono 404. Aggiunti i tipi `.jpeg`, `.xml`, `.txt` e gli header `X-Content-Type-Options: nosniff` e `Referrer-Policy`. Il server serve solo per lavorare in locale e non viene pubblicato.

**Cosa non si può fare con GitHub Pages**
GitHub Pages non permette di impostare header HTTP personalizzati. Restano quindi fuori:
- la protezione anti-clickjacking (`frame-ancestors` / `X-Frame-Options`), che non funziona tramite `<meta>`;
- `Permissions-Policy` e una CSP come header vero.

Per un sito solo informativo, senza login né moduli, il rischio concreto è basso. Se in futuro servissero, si può mettere il sito dietro Cloudflare o pubblicarlo su Netlify o Cloudflare Pages, dove gli header si configurano. HSTS è già attivo di default sui domini `github.io`.

**Da fare tu su GitHub, prima e dopo la pubblicazione**
1. Account GitHub con **autenticazione a due fattori** attiva: chi entra nell'account può cambiare il sito.
2. Nel repository → Settings → Pages: spuntare **"Enforce HTTPS"**.
3. Con un dominio proprio: **verificare il dominio** in GitHub (Settings → Pages → *Verified domains*), per evitare che qualcun altro lo agganci a un suo sito; impostare i record DNS esattamente come indicato da GitHub e non lasciare record che puntano a GitHub dopo un'eventuale dismissione.
4. Non caricare mai nel repository file con password, chiavi, dati di iscritti o documenti: il repository di un sito GitHub Pages gratuito è **pubblico**.
5. Non aggiungere `.nojekyll`: disattiverebbe l'esclusione dei file interni fatta con `_config.yml`.
6. Dopo la pubblicazione, controllare che `…/README.md`, `…/NOTE-PLACEHOLDER.md` e `…/serve.ps1` diano 404.

## Pubblicazione, SEO e prestazioni (15/09/2026)
- **Indirizzo base del sito**: `https://www.asdrevolutionsport.it/` (dominio proprio, attivo dal 15/09/2026). È scritto nei `canonical`, `og:url` e `og:image` di 6 pagine, nel JSON-LD di `index.html`, in `robots.txt` e in `sitemap.xml`. Se il dominio cambiasse, cercarlo in tutti i file e sostituirlo.
- **Dominio e DNS (verificati il 17/09/2026)**:
  - dominio `asdrevolutionsport.it`, acquistato tramite GoDaddy; nel Registro .it il registrar è **Key-Systems GmbH**, usato da GoDaddy per i .it; scadenza **15/09/2027** (tenere attivo il rinnovo automatico);
  - DNS su GoDaddy: `ns81`/`ns82.domaincontrol.com`;
  - `asdrevolutionsport.it` (senza www) punta con 4 record A ai server GitHub Pages `185.199.108–111.153`, mentre `www` è un CNAME verso `daniele-arch1.github.io`. **Nessun inoltro GoDaddy**: tutte le richieste arrivano a GitHub (server `GitHub.com`), che reindirizza HTTP e dominio senza www a `https://www.asdrevolutionsport.it/`;
  - certificato HTTPS valido per il dominio, rinnovato automaticamente da GitHub (quello verificato scade il 15/12/2026);
  - il vecchio indirizzo `daniele-arch1.github.io/assd-revolution-0/` reindirizza al dominio.
- **Cosa non toccare su GoDaddy**: i 4 record A, il CNAME `www` e i nameserver. Non attivare l'inoltro del dominio né il sito o la pagina "parcheggio" di GoDaddy: cambierebbe il funzionamento del sito e renderebbe inesatta l'informativa (punto 3.6).
- **Titoli**: tutti contengono "Gorizia" (in home: "Palestra a Gorizia — A.S.D. Revolution Sport").
- **`<h1>` in `abbonati.html`**: presente ma nascosto alla vista (classe `.visually-hidden`), per non cambiare la grafica. Lo leggono motori di ricerca e screen reader.
- **Dati strutturati (JSON-LD, tipo `ExerciseGym`)** in `index.html`: nome, indirizzo, telefono, email, orari, social. **Se cambiano orari, telefono o indirizzo vanno aggiornati anche qui**, oltre che in `HOURS` di `js/main.js` e nei footer.
- **Open Graph**: anteprima per WhatsApp/Facebook su 6 pagine, immagine `hero scorrimento 1.jpeg`.
- **Logo nei risultati di Google**:
  - *Favicon accanto al risultato*: `img/favicon.png` a **96×96px**, perché Google accetta solo favicon quadrate multiple di 48px (la precedente da 64px non era valida). Il link `<link rel="icon" sizes="96x96">` è in tutte le pagine.
  - *Logo nei dati strutturati*: il JSON-LD di `index.html` punta a `img/logo-512.jpg` (512×512px; Google chiede almeno 112px). Lo scaricano solo i motori di ricerca, non i visitatori.
  - Google mostra **una sola favicon per nome host**: con il dominio proprio `www.asdrevolutionsport.it` userà quella della palestra. Il logo che compare nelle ricerche locali e su Maps viene invece dalla **scheda Google Business Profile** della palestra: va caricato lì.
  - Aggiornamento in Google: dopo la pubblicazione, da Search Console chiedere l'indicizzazione della home e inviare `sitemap.xml`. Google aggiorna favicon e logo con i suoi tempi, anche settimane.
- **`google6de821c45435e833.html`**: è il file di verifica della proprietà su Google Search Console. **Non va modificato né cancellato**, altrimenti la verifica decade, e deve restare pubblicato (non è in `_config.yml`).
- **`recensioni.html`**: `noindex, follow` ed esclusa dalla sitemap (duplica la home). **`404.html`**: `noindex`.
- **`robots.txt`**: con il dominio proprio si trova nella radice (`https://www.asdrevolutionsport.it/robots.txt`), quindi i motori di ricerca lo leggono. Su **Google Search Console** conviene comunque aggiungere la proprietà del dominio `asdrevolutionsport.it` (o `https://www.asdrevolutionsport.it/`) e inviare lì la sitemap: la proprietà verificata su `github.io` non vale per il nuovo dominio.
- **`404.html`**: GitHub Pages la mostra per gli indirizzi inesistenti. I link relativi funzionano per gli indirizzi di primo livello (es. `/pagina-inesistente.html`), non per percorsi annidati.
- **File interni esclusi dal sito pubblicato**: `_config.yml` esclude `NOTE-PLACEHOLDER.md`, `README.md`, `serve.ps1` e `img/logo.webp`, che restano nel repository ma non vengono serviti online. Funziona perché GitHub Pages usa Jekyll: **non aggiungere un file `.nojekyll`**, altrimenti l'esclusione smette di funzionare. Su un hosting diverso quei file semplicemente non vanno caricati.
- **Logo alleggerito**: l'header usa `img/logo-header.jpg` (132px, 11 KB, su fondo bianco come il cerchio dell'header) e la favicon `img/favicon.png` (96px, 17,7 KB), al posto di `img/logo.webp` (1254px, 182 KB) che resta solo come file sorgente. Se il logo cambia, vanno rigenerate le due versioni piccole.
- **Foto**: `width`/`height` reali su tutte le immagini di contenuto (evita che la pagina "salti" durante il caricamento); `loading="lazy"` sulle foto di `foto.html` tranne la prima, che è visibile subito. Il banner di `macchinari.html` ha `height: auto` nel CSS, necessario perché gli attributi non lo deformino su mobile. Rimosso il `src=""` vuoto dalla lightbox.
- **Cookie**: il testo del banner ora cita Google Maps e spiega cosa fanno "Accetta" e "Rifiuta". Il pulsante **"Gestisci cookie"** nel footer di ogni pagina riapre il banner; se si rifiuta dopo aver accettato, la mappa si spegne subito (`unloadGymMap` in `js/main.js`). I punti 4, 11.4 e 11.5 dell'informativa lo descrivono.
- **Recensioni e rotella del mouse**: tolto `overscroll-behavior-y: contain` dalle card, che bloccava lo scroll della pagina con il mouse. Card alte 420px (520px su mobile); lo scroll interno si attiva solo se il testo sfora più del padding inferiore, e `justify-content: safe center` evita che un testo molto lungo venga tagliato in alto.
- **Pallini dei caroselli**: area tocco 24×24px (vedi Note tecniche).
- **Nota sull'anteprima locale**: la console mostra un 404 su `/favicon.ico`. È una richiesta automatica del browser alla radice del server locale ed è innocua: la favicon vera (`img/favicon.png`) viene caricata correttamente.

## Diagnostica (11/09/2026, aggiornata)
Eseguito un controllo completo su tutte e 6 le pagine (incluse `profilo.html` e il carosello hero, aggiunte dopo il primo giro): link interni/anchor, percorsi immagini su disco, ID duplicati, alt mancanti, placeholder testuali dimenticati, coerenza `target="_blank"`/`rel="noopener"`, coerenza filtro/categoria macchinari (7 categorie, 31 macchinari, nessuna orfana), console ed errori di rete, filtro macchinari, carosello recensioni (avanti/indietro/loop), lightbox foto (apertura/chiusura), carosello hero (automatico/manuale), tab e blocco invio form in "Profilo", menu mobile (apertura/chiusura/click fuori), overflow orizzontale a 1440px/980px/375px. Nessun errore residuo trovato.

## Modifiche più recenti (11/09/2026, secondo giro)
- Home/hero: rimosso il testo introduttivo e il pulsante "Dove si trova"; indirizzo spostato in basso a sinistra sull'hero solo su desktop (nascosto su mobile); su mobile rimosse le frecce di scorrimento manuale del carosello hero e di quello recensioni, sostituite da un vero swipe touch (funziona anche sul carosello recensioni).
- Layout mobile in generale reso più arioso (più spaziatura tra sezioni).
- Emoji rimosse dalle card macchinari (`macchinari.html`, 31 card) e dalle card servizi (`abbonati.html`, 8 card): ora sono solo testo.
- "Corsi ed eventi" spostato dalla home a una pagina dedicata `aggiornamenti.html`; link di navbar/footer aggiornati su tutte le 8 pagine.
- Rimossa la card finale ripetitiva "Pronto a iniziare ad allenarti?" da `abbonati.html` (vedi nota sopra sui pulsanti Chiamaci/Scrivici/Paga online).
- `abbonati.html`: sotto i 760px "Servizi inclusi" e "Abbonamenti" mostrano una card in evidenza (rispettivamente "Accesso alla sala pesi" e "Mensile €45/mese") seguita da una scheda a comparsa apribile con un pulsante ("Vedi tutti i servizi" / "Vedi tutti gli abbonamenti", vedi nota sopra); le card della griglia restano quelle originali, solo nascoste finché non si apre.
- Link "Lascia una recensione su Google" (home + `recensioni.html`) impostato sulla pagina di ricerca Google della palestra, in attesa del placeid per il link diretto.
- Rimosso il sottotitolo "Un unico abbonamento, tutto compreso..." sotto il titolo di `abbonati.html`.
- Rimosso interamente il banner in cima a `abbonati.html` (breadcrumb "Home / Servizi" e titolo "Cosa include il tuo abbonamento"): la pagina ora inizia direttamente con la sezione Abbonamenti. Il primo `<section>` ha uno spazio extra in cima (`padding-top` inline che tiene conto dell'altezza della navbar fissa) per non finire nascosto sotto il menu.
- **Carosello recensioni** (home + `recensioni.html`): rifinito. Prima ogni recensione sostituiva il contenuto di scatto e la card cambiava altezza in base alla lunghezza del testo; ora tutte le recensioni sono precaricate come schede sovrapposte della stessa dimensione fissa, con una transizione di dissolvenza + scorrimento laterale (nella direzione corretta: avanti o indietro, anche saltando con i pallini) quando si cambia recensione — stessa tecnica del carosello foto in hero, adattata con uno scorrimento orizzontale più marcato. Altezza fissa: 400px su desktop/tablet, 480px su mobile (sotto 760px, dove il testo va a capo su più righe) — con questi valori, nella pratica, nessuna delle 5 recensioni attuali ha bisogno di scorrimento interno. Lo scroll verticale interno (`overflow-y`) viene attivato via JS **solo** sulle card che superano davvero lo spazio disponibile (`js/main.js`, funzione `updateReviewScrollability`): questo evita che su mobile un dito che scorre la pagina partendo sopra una recensione "vuota" resti intrappolato in una card scrollabile ma senza nulla da scorrere — un problema reale riscontrato in produzione, non solo teorico. Se in futuro aggiungi una recensione molto più lunga delle attuali e supera comunque lo spazio fisso, diventerà automaticamente scorribile solo lei (con una barra sottile), senza mai tagliare il testo.
- In `abbonati.html` la sezione "Abbonamenti" ora viene prima di "Servizi inclusi" (era il contrario); l'ancora `abbonati.html#servizi` (link "Servizi" in navbar) punta ancora correttamente alla sezione Servizi, ora in seconda posizione.
- **Mappa** (home, sezione "Dove si trova"): rimosso il pulsante "Ottieni indicazioni stradali / Apri Maps" che avevamo aggiunto sopra l'embed — copriva i controlli nativi di Google Maps (zoom, mappa satellitare, tasto indicazioni) in basso/nell'angolo. La scheda del luogo con l'icona indicazioni stradali integrata di Google (in alto a sinistra sulla mappa) è già sufficiente.
- **Corsi ed eventi** (`aggiornamenti.html`): al momento non c'è più nessuna card corso/evento in pagina — è stata rimossa anche la card testuale "Circuit Training" (oltre alla locandina, tolta in precedenza: anche il file `img/Circuit training corso.png`, 2,1 MB, è stato eliminato perché non più usato; una copia è nei backup), perché non c'era ancora una data/scheda corso definitiva da mostrare. Anche il sottotitolo sotto il titolo ("Uno spazio dedicato alle attività...") è stato tolto. La pagina ora va dal banner in cima (solo titolo "Corsi ed eventi", breadcrumb) direttamente a una sezione "Seguici" identica a quella della home (stessi pulsanti Facebook/Instagram, stesso stile `.social-links`/`.social-btn`), per invitare a seguire i social dove verranno pubblicate le locandine e le date dei prossimi corsi. Quando ci sarà un corso/evento reale e confermato da mostrare, si può aggiungere una nuova `<article class="event-card">` dentro un `<div class="events-grid is-single">` (o `events-grid` senza `is-single` se le card diventano più di una) subito dopo il banner, prima della sezione "Seguici" — la classe `is-single` centra una singola card, pensata in origine per la locandina verticale.
- **Macchinari**: aggiunto "Hack squat" alla categoria Gambe (ora 32 macchinari totali).
- Aggiunto `color-scheme: light` (meta + CSS) su tutte le pagine per evitare che alcuni telefoni forzino un tema scuro sugli elementi nativi (vedi Note tecniche).
- Verificato su Browser pane: schermo 375px (mobile, anche con tema scuro di sistema emulato) e 1440px/800px (desktop), nessun errore in console.
